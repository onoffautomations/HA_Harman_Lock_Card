name: Hassfest (disabled - this repo is a Lovelace card)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: This repo is a Lovelace card (plugin)
        run: |
          echo "This repository is a Lovelace card, not a Home Assistant integration."
          echo "Hassfest is for custom_components integrations, so it is disabled here."
          echo "Use HACS validation (plugin) instead."
